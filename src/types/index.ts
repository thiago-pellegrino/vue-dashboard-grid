declare module "vue-dashboard-grid" {
    import { Component } from "vue";
  
    const Dashboard: Component;
    const DashItem: Component;
    const DashLayout: Component;
    const DashGrid: Component;
  
    export { DashGrid, Dashboard, DashItem, DashLayout };
}